//Armstromg or not
#include<stdio.h>
int main()
{
	int n,sum=0,a,r;
	printf("Enter the value of n:");
	scanf("%d",&n);
	for(a=n;a>0;a/=10)
	{
		r=a%10;
		sum=sum+r*r*r;
	}
	if(sum==n)
	printf("Armstrong");
	else
	printf("Not Armstrong");
	return 0;
}
